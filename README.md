# PassGen
PassGen is a secure password generator web app that creates cryptographically strong passwords using crypto.getRandomValues(). It features entropy-based strength analysis, customizable character sets, session history, and a responsive UI.
